# Airswift ATS - Comprehensive Integration & User Guide

## 📋 Table of Contents

1. [User Guide](#user-guide)
2. [Frontend Developer Guide](#frontend-developer-guide)
3. [Admin Features Guide](#admin-features-guide)
4. [Complete Feature List](#complete-feature-list)
5. [Security & Best Practices](#security--best-practices)

---

# USER GUIDE

## 🎯 Getting Started with Airswift

### What is Airswift?

Airswift is a modern Applicant Tracking System (ATS) that helps job seekers find opportunities and manage their applications in one place. Our platform features AI-powered CV analysis, intelligent job matching, and an innovative interview process.

---

## 📝 Account Setup

### Registration

1. **Visit the Website**: Go to the Airswift portal
2. **Click "Sign Up"**
3. **Enter Your Details**:
   - Email address
   - First name and last name
   - Password (secure, 8+ characters)
4. **Verify OTP**: Enter the 6-digit code sent to your email
5. **Complete Profile**: Add your personal information and upload a CV

### Login

**Option 1: Email & Password**
- Enter your email and password
- Click "Login"

**Option 2: One-Time Password (OTP)**
- Enter your email
- Click "Login with OTP"
- Enter the 6-digit code sent to your email
- Click "Verify"

### Password Management

- **Forgot Password**: Click "Forgot Password" on login page, enter email, and reset via link
- **Change Password**: Go to Settings → Security → Change Password
- **Account Recovery**: Use your registered email for account recovery

---

## 💼 Profile Management

### Complete Your Profile

1. **Personal Information**
   - Upload a professional profile photo
   - Add contact phone number
   - Specify your location/timezone
   - Add LinkedIn profile URL (optional)

2. **Professional Information**
   - Add skills (start typing to search existing skills)
   - Enter work experience with company, role, duration
   - Add education history
   - Link to portfolio or personal website

3. **Upload CV**
   - Upload your primary resume/CV
   - System will automatically extract skills and experience
   - View extracted information and make corrections
   - You can upload multiple CVs (optional)

4. **Job Preferences**
   - Select job categories you're interested in
   - Specify desired salary range
   - Indicate available start date
   - Select preferred work modes (On-site, Remote, Hybrid)

### View Your Profile

- Click your name → "My Profile" to view your complete profile
- See how AI analyzes your CV
- View your profile completion percentage
- Get suggestions to improve your profile

---

## 🔍 Finding Jobs

### Browse Available Jobs

1. **Home Page**
   - See featured job listings
   - View newest job postings
   - Browse jobs by category

2. **Search Jobs**
   - Use the search bar for job titles
   - Filter by location, salary, experience level
   - Sort by date posted, salary, or relevance
   - Save jobs for later

3. **Job Categories**
   - Click on job categories (IT, Healthcare, Finance, etc.)
   - View all jobs in that category
   - Refine results with additional filters

### View Job Details

- **Job Title & Company**
- **Location & Work Type** (On-site, Remote, Hybrid)
- **Salary Range** (if disclosed)
- **Job Description** with key responsibilities
- **Required Skills & Experience**
- **Application Deadline**
- **Similar Jobs** (recommendations)

### Save & Apply

- **Save Job**: Click ❤️ to save for later
- **Apply Now**: Click "Apply Now" button
- **View Saved Jobs**: Go to "Saved Jobs" in your profile

---

## 📤 Applying for Jobs

### Step 1: Basic Application

When you click "Apply Now":
1. System loads your primary CV automatically
2. Review extracted information
3. Add a cover letter (optional but recommended)
4. Select additional documents to include

### Step 2: Document Upload

Upload additional documents:
- **Passport/ID**: For visa processing jobs
- **National ID**: Required for some positions
- **Certificates**: Professional certifications
- **Additional CV**: Alternative resume versions
- **Cover Letter**: Upload as PDF or enter text

### Step 3: Review & Submit

- Review all information before submitting
- Confirm all required documents are included
- Check for any validation errors
- Click "Submit Application"

### Application Confirmation

- You'll receive confirmation on screen
- Email confirmation sent to your inbox
- Application now visible in "My Applications"
- Recruiter begins reviewing your application

---

## 📊 Managing Applications

### View All Applications

1. Go to **"My Applications"**
2. See all submitted applications with status
3. Filter by job title, company, or status
4. Sort by date applied or deadline
5. Search for specific applications

### Application Statuses

- **Submitted**: Application received and pending review
- **Under Review**: Recruiter reviewing your CV/profile
- **Shortlisted**: You've passed initial screening
- **Interview Scheduled**: Interview has been scheduled
- **Interview Completed**: Interview finished, awaiting feedback
- **Offer Extended**: Job offer received
- **Rejected**: Application not successful
- **Withdrawn**: You withdrew the application

### Track Application Progress

- **Timeline View**: See important dates and milestones
- **Status Updates**: Get notifications when status changes
- **Next Steps**: View what action is needed next
- **Recruiter Messages**: View communications from recruiter

### Interview Requests

When shortlisted:
1. **View Interview Invite**: See interview details in application
2. **Check Time & Date**: Confirm with your schedule
3. **Select Interview Type**: 
   - Video interview
   - Voice AI interview
   - In-person interview
4. **Confirm Availability**: Click "Accept" to confirm
5. **Request Reschedule**: If time doesn't work, request new time slot

---

## 🎤 Interview Process

### Scheduled Video Interviews

1. **Before Interview**:
   - Check internet connection and camera/microphone
   - Use modern browser (Chrome, Firefox, Safari)
   - Test video/audio in settings
   - Join 5 minutes early

2. **During Interview**:
   - Look at camera, not screen
   - Speak clearly
   - Take brief notes if needed
   - Answer thoroughly but concisely

3. **After Interview**:
   - You'll receive feedback email shortly
   - View score and recruiter comments
   - Application moves to next stage or concludes

### AI-Powered Voice Interviews

1. **What to Expect**:
   - You'll see questions presented in text
   - Record your voice response
   - AI analyzes content, clarity, and confidence
   - Typically 5-10 minutes

2. **How to Prepare**:
   - Ensure quiet environment
   - Test microphone before starting
   - Practice answering interview questions
   - Have CV handy for reference

3. **During Interview**:
   - Read question carefully
   - Click to record your response
   - Speak naturally and clearly
   - Take time to provide complete answer

4. **Results**:
   - AI provides immediate score
   - View detailed feedback on your responses
   - See how you compare to other candidates
   - Recruiter reviews and makes decision

### Interview Reschedule

If scheduled time doesn't work:
1. Go to application
2. Click "Request Reschedule"
3. Select "Reason for Reschedule"
4. Choose new date/time(s)
5. Add optional message
6. Click "Submit Request"
7. Recruiter will confirm new time

---

## 💳 Payments (If Applicable)

### Application Fees

- Some premium jobs may require application fee
- Fee amount clearly shown before applying
- Payment processed securely via Stripe/PayPal
- Receipt emailed to you

### Interview Fees

- Some interviews may have associated fees
- Clearly disclosed in interview invitation
- Optional to participate (other opportunities available)
- Payment required to schedule

### Visa Processing Services

- Optional paid service for visa assistance
- Available for eligible visa-sponsored jobs
- Cost varies by country and visa type
- Includes document preparation and guidance

### Payment History

View all payments in **"Payments"** section:
- Payment date and amount
- Service or job applied for
- Receipt download option
- Refund requests (if eligible)

---

## 📢 Notifications & Communication

### Email Notifications

You'll receive emails for:
- **Application Updates**: Status changes, shortlisting
- **Interview Invites**: Scheduled interviews and reschedules
- **New Job Matches**: Jobs matching your preferences
- **Recruiter Messages**: Communications from recruiters
- **System Updates**: Important platform announcements
- **Account Activity**: Login alerts, password changes

### In-App Notifications

- Notification bell 🔔 shows unread messages
- View all notifications in Notification Center
- Mark as read or delete
- Click to view full details

### Notification Preferences

Customize in **Settings → Notifications**:
- Choose which types to receive
- Email vs. in-app notifications
- Opt-out of specific categories
- Set quiet hours

---

## 🎯 Job Recommendations

### Get Matched Jobs

- Platform AI analyzes your CV and preferences
- Recommends jobs suited to your skills
- View similarity score for each recommendation
- Apply directly recommended jobs

### How Matching Works

AI considers:
- Your skills and experience level
- Salary expectations
- Location preferences
- Job category interests
- Career history and growth path

### View Recommendations

1. Go to **"For You"** tab
2. See personalized job recommendations
3. View match percentage for each job
4. Expand for full details
5. Click "Apply" or "Save" for later

---

## 🔒 Security & Privacy

### Data Protection

- Your data is encrypted and secure
- Only recruiters viewing your application can see your details
- No data sold to third parties
- Compliant with GDPR and privacy regulations

### Privacy Settings

- Go to **Settings → Privacy**
- Control who can view your profile
- Choose resume visibility
- Manage third-party access

### Account Security

- Change password regularly
- Use strong passwords (8+ chars, numbers, symbols)
- Enable two-factor authentication (if available)
- Don't share login credentials
- Log out after using shared computers

### Report Abuse

Found suspicious activity?
1. Click profile icon → **Settings**
2. Go to **"Report a Problem"**
3. Select issue type and provide details
4. Our team will investigate within 24 hours

---

## ❓ FAQs for Users

**Q: How do I upload a new CV?**
A: Go to Profile → CV → Upload new file. You can have multiple CVs and select which to use per application.

**Q: Can I withdraw an application?**
A: Yes, go to My Applications, click on the job, and click "Withdraw Application" (available before offer stage).

**Q: How long does CV review take?**
A: Usually 24-48 hours, but can vary. You'll be notified when status changes.

**Q: What if I miss an interview?**
A: Contact recruiter immediately via email/message. You may be offered to reschedule (recruiter's discretion).

**Q: How do I change my contact information?**
A: Go to Settings → Account Details and update your email/phone. Verify new email with OTP.

**Q: Can I apply for the same job twice?**
A: No, one application per job per user. You can request to update your application if allowed.

**Q: What are interview fees for?**
A: Fees help us conduct personalized interviews. Optional - other opportunities available without fees.

---

# FRONTEND DEVELOPER GUIDE

## 🚀 Quick Start

### Environment Setup

1. **Backend URL**: `http://localhost:5000` (development) or your deployed URL
2. **WebSocket URL**: Same as backend URL for real-time features
3. **CORS**: Configured to allow `https://airswift-frontend.vercel.app` in production

### Required Dependencies

```bash
npm install socket.io-client axios jwt-decode
# or
yarn add socket.io-client axios jwt-decode
```

---

## 🔐 Authentication

### Registration Flow

```javascript
// 1. Register user
const registerResponse = await fetch('/api/auth/register', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'user@example.com',
    password: 'password123',
    firstName: 'John',
    lastName: 'Doe'
  })
});

// 2. Verify OTP
const otpResponse = await fetch('/api/auth/verify-otp', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'user@example.com',
    otp: '123456'
  })
});
```

### Login Flow

```javascript
// Traditional login
const loginResponse = await fetch('/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'user@example.com',
    password: 'password123'
  })
});

// OTP Login
const otpLoginResponse = await fetch('/api/auth/send-login-otp', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ email: 'user@example.com' })
});

// Verify OTP
const verifyResponse = await fetch('/api/auth/verify-login-otp', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    email: 'user@example.com',
    otp: '123456'
  })
});
```

### Token Management

```javascript
// Store tokens after login
const { accessToken, refreshToken } = await loginResponse.json();
localStorage.setItem('accessToken', accessToken);
localStorage.setItem('refreshToken', refreshToken);

// Include in requests
const headers = {
  'Authorization': `Bearer ${localStorage.getItem('accessToken')}`,
  'Content-Type': 'application/json'
};

// Refresh token
const refreshResponse = await fetch('/api/auth/refresh', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ refreshToken: localStorage.getItem('refreshToken') })
});
```

---

## 🌐 API Configuration

### Base URL
```javascript
const API_BASE = process.env.REACT_APP_API_URL || 'http://localhost:5000';
```

### Common Headers
```javascript
const getAuthHeaders = () => ({
  'Authorization': `Bearer ${localStorage.getItem('accessToken')}`,
  'Content-Type': 'application/json'
});
```

### Axios Instance Setup
```javascript
import axios from 'axios';

const api = axios.create({
  baseURL: API_BASE,
  timeout: 10000,
});

api.interceptors.request.use((config) => {
  const token = localStorage.getItem('accessToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

api.interceptors.response.use(
  (response) => response,
  async (error) => {
    if (error.response?.status === 401) {
      // Token expired, try refresh
      const refreshToken = localStorage.getItem('refreshToken');
      if (refreshToken) {
        try {
          const refreshResponse = await axios.post('/api/auth/refresh', {
            refreshToken
          });
          const { accessToken } = refreshResponse.data;
          localStorage.setItem('accessToken', accessToken);
          // Retry original request
          error.config.headers.Authorization = `Bearer ${accessToken}`;
          return axios(error.config);
        } catch (refreshError) {
          // Refresh failed, logout
          localStorage.removeItem('accessToken');
          localStorage.removeItem('refreshToken');
          window.location.href = '/login';
        }
      }
    }
    return Promise.reject(error);
  }
);
```

---

## 📋 Core API Endpoints

### Jobs

```javascript
// Get all jobs (public)
const jobs = await api.get('/api/jobs');

// Get job details
const job = await api.get(`/api/jobs/${jobId}`);

// Apply for job (authenticated)
const formData = new FormData();
formData.append('cv', cvFile);
formData.append('coverLetter', coverLetter);
formData.append('jobId', jobId);

const application = await api.post('/api/applications/apply', formData, {
  headers: { 'Content-Type': 'multipart/form-data' }
});
```

### Applications

```javascript
// Get user's applications
const applications = await api.get('/api/applications/my');

// Get specific application details
const application = await api.get(`/api/applications/${applicationId}`);

// Upload additional documents
const docFormData = new FormData();
docFormData.append('passport', passportFile);
docFormData.append('nationalId', nationalIdFile);
docFormData.append('certificates', certificatesFile);

await api.post('/api/applications/upload-documents', docFormData);

// Withdraw application
await api.post(`/api/applications/${applicationId}/withdraw`);
```

### Interviews

```javascript
// Get user's interviews
const interviews = await api.get('/api/interviews/my');

// Get interview details
const interview = await api.get(`/api/interviews/${interviewId}`);

// AI Interview interaction
const aiResponse = await api.post('/api/interviews/ask', {
  question: 'Tell me about your experience',
  sessionId: interviewSessionId
});

// Submit interview response
await api.post('/api/interviews/submit-response', {
  interviewId,
  questionId,
  response: 'My answer...'
});

// Request interview reschedule
await api.post(`/api/interviews/${interviewId}/reschedule`, {
  reason: 'Conflict with another interview',
  suggestedTimes: ['2024-04-15T10:00', '2024-04-15T14:00']
});
```

### Profile Management

```javascript
// Get profile
const profile = await api.get('/api/profile');

// Update profile
await api.put('/api/profile', {
  firstName: 'Updated Name',
  phone: '1234567890',
  location: 'Toronto',
  skills: ['JavaScript', 'React'],
  experience: '5 years'
});

// Upload CV
const cvFormData = new FormData();
cvFormData.append('cv', cvFile);
await api.post('/api/profile/upload-cv', cvFormData);

// Upload profile photo
const photoFormData = new FormData();
photoFormData.append('photo', photoFile);
await api.post('/api/profile/upload-photo', photoFormData);
```

---

## 📁 File Upload Handling

### CV and Document Uploads

```javascript
const handleFileUpload = async (file, endpoint) => {
  const formData = new FormData();
  formData.append('file', file);

  try {
    const response = await api.post(endpoint, formData, {
      headers: { 'Content-Type': 'multipart/form-data' },
      onUploadProgress: (progressEvent) => {
        const percentCompleted = Math.round(
          (progressEvent.loaded * 100) / progressEvent.total
        );
        console.log(`Upload progress: ${percentCompleted}%`);
      }
    });
    return response.data;
  } catch (error) {
    console.error('Upload failed:', error);
    throw error;
  }
};

// Usage
const cvUpload = await handleFileUpload(cvFile, '/api/profile/upload-cv');
const applicationUpload = await handleFileUpload(cvFile, '/api/applications/apply');
```

---

## 🔴 Real-Time WebSocket Integration

### Socket.io Setup

```javascript
import io from 'socket.io-client';

class SocketManager {
  constructor() {
    this.socket = null;
    this.isConnected = false;
  }

  connect() {
    this.socket = io(API_BASE, {
      auth: {
        token: localStorage.getItem('accessToken')
      },
      transports: ['websocket', 'polling']
    });

    this.socket.on('connect', () => {
      console.log('Connected to server');
      this.isConnected = true;
    });

    this.socket.on('disconnect', () => {
      console.log('Disconnected from server');
      this.isConnected = false;
    });

    this.setupEventListeners();
  }

  setupEventListeners() {
    // Application events
    this.socket.on('newApplication', (data) => {
      console.log('New application:', data);
      // Update UI with new application
    });

    this.socket.on('applicationUpdate', (data) => {
      console.log('Application status update:', data);
      // Update application status in UI
    });

    // CV Scoring events
    this.socket.on('cvScoringComplete', (data) => {
      console.log('CV scoring complete:', data);
      // Update CV score display
    });

    // Interview events
    this.socket.on('interviewScheduled', (data) => {
      console.log('Interview scheduled:', data);
      // Show interview notification
    });

    // Email events
    this.socket.on('emailSent', (data) => {
      console.log('Email sent:', data);
      // Show email sent confirmation
    });

    // Dashboard updates
    this.socket.on('dashboardUpdate', (data) => {
      console.log('Dashboard update:', data);
      // Refresh dashboard stats
    });
  }

  disconnect() {
    if (this.socket) {
      this.socket.disconnect();
    }
  }
}

export const socketManager = new SocketManager();
```

---

## 💰 Payment Integration

```javascript
// Initiate payment
const paymentInit = await api.post('/api/payment/initiate', {
  amount: 50000, // in cents
  currency: 'USD',
  description: 'Job application fee'
});

// Verify payment
const paymentVerify = await api.post('/api/payment/verify', {
  paymentId: paymentInit.data.paymentId,
  transactionId: 'txn_123456'
});

// Get payment history
const payments = await api.get('/api/payments/history');
```

---

## 📊 Dashboard & Analytics

### User Dashboard

```javascript
// Get user dashboard summary
const dashboard = await api.get('/api/dashboard/user/summary');
// Returns: applications count, interviews scheduled, offers, recommendations

// Get all user applications with stats
const appStats = await api.get('/api/applications/stats');
// Returns: breakdown by status (pending, shortlisted, interview, etc.)

// Get recommended jobs
const recommendations = await api.get('/api/recommendations');
// Returns: personalized job recommendations with match scores
```

### Real-time Dashboard Updates

```javascript
socket.on('dashboardUpdate', (data) => {
  // Update dashboard with real-time stats
  updateDashboardStats(data.stats);
});

socket.on('applicationUpdate', (data) => {
  // Application status changed
  updateApplicationStatus(data.applicationId, data.newStatus);
});

socket.on('interviewScheduled', (data) => {
  // New interview scheduled
  addInterviewNotification(data);
});
```

---

## 🚨 Error Handling

### Common Error Responses

```javascript
const handleApiError = (error) => {
  if (error.response) {
    const { status, data } = error.response;

    switch (status) {
      case 400:
        // Bad request - validation error
        showError(data.message || 'Invalid request data');
        break;
      case 401:
        // Unauthorized - token expired
        localStorage.removeItem('accessToken');
        window.location.href = '/login';
        break;
      case 403:
        // Forbidden - insufficient permissions
        showError('You do not have permission to perform this action');
        break;
      case 404:
        // Not found
        showError('Resource not found');
        break;
      case 429:
        // Rate limited
        showError('Too many requests. Please try again later');
        break;
      case 500:
        // Server error
        showError('Server error. Please try again later');
        break;
      default:
        showError('An unexpected error occurred');
    }
  } else if (error.request) {
    // Network error
    showError('Network error. Please check your connection');
  } else {
    // Other error
    showError('An unexpected error occurred');
  }
};
```

### Rate Limiting

```javascript
if (error.response?.status === 429) {
  const retryAfter = error.response.headers['retry-after'];
  setTimeout(() => {
    // Retry the request
  }, retryAfter * 1000);
}
```

---

## 🔧 Development Best Practices

### State Management

```javascript
// Redux slice example for applications
import { createSlice, createAsyncThunk } from '@reduxjs/toolkit';

export const fetchApplications = createAsyncThunk(
  'applications/fetchApplications',
  async (_, { rejectWithValue }) => {
    try {
      const response = await api.get('/api/applications/my');
      return response.data;
    } catch (error) {
      return rejectWithValue(error.response.data);
    }
  }
);

const applicationsSlice = createSlice({
  name: 'applications',
  initialState: {
    applications: [],
    loading: false,
    error: null
  },
  reducers: {
    // Socket updates
    updateApplicationStatus: (state, action) => {
      const { applicationId, status } = action.payload;
      const application = state.applications.find(app => app.id === applicationId);
      if (application) {
        application.status = status;
      }
    }
  },
  extraReducers: (builder) => {
    builder
      .addCase(fetchApplications.pending, (state) => {
        state.loading = true;
      })
      .addCase(fetchApplications.fulfilled, (state, action) => {
        state.loading = false;
        state.applications = action.payload;
      })
      .addCase(fetchApplications.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload;
      });
  }
});
```

### Environment Variables

```javascript
// .env.local
REACT_APP_API_URL=http://localhost:5000
REACT_APP_SOCKET_URL=http://localhost:5000
REACT_APP_CLOUDINARY_CLOUD_NAME=your_cloud_name
REACT_APP_MODE=development
```

---

# ADMIN FEATURES GUIDE

## Overview

This guide helps frontend developers implement the complete admin dashboard and features for managing the Airswift recruitment process.

---

## 🔧 System Settings Management

### API Endpoints
```javascript
// Get all settings
GET /api/admin/settings
Authorization: Bearer {token}

// Get specific setting
GET /api/admin/settings/:key

// Create setting
POST /api/admin/settings
{ "key": "email_from_address", "value": "noreply@airswift.com" }

// Update setting
PUT /api/admin/settings/:key
{ "value": "updated_value" }

// Delete setting
DELETE /api/admin/settings/:key
```

### Frontend Implementation
```javascript
import axios from 'axios';

const api = axios.create({
  baseURL: process.env.REACT_APP_API_URL,
  headers: {
    'Authorization': `Bearer ${localStorage.getItem('accessToken')}`
  }
});

export const fetchSettings = async () => {
  try {
    const response = await api.get('/admin/settings');
    return response.data.settings;
  } catch (error) {
    console.error('Error fetching settings:', error);
    throw error;
  }
};

export const updateSetting = async (key, value) => {
  try {
    const response = await api.put(`/admin/settings/${key}`, { value });
    return response.data.setting;
  } catch (error) {
    console.error('Error updating setting:', error);
    throw error;
  }
};
```

### UI Component
```jsx
import React, { useState, useEffect } from 'react';
import { fetchSettings, updateSetting } from './adminApi';

export const SettingsPanel = () => {
  const [settings, setSettings] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    loadSettings();
  }, []);

  const loadSettings = async () => {
    try {
      const data = await fetchSettings();
      setSettings(data);
    } finally {
      setLoading(false);
    }
  };

  const handleUpdate = async (key, newValue) => {
    await updateSetting(key, newValue);
    await loadSettings();
  };

  if (loading) return <div>Loading settings...</div>;

  return (
    <div className="settings-panel">
      {settings.map(setting => (
        <div key={setting.key} className="setting-item">
          <label>{setting.key}</label>
          <input 
            value={setting.value} 
            onChange={(e) => handleUpdate(setting.key, e.target.value)}
          />
        </div>
      ))}
    </div>
  );
};
```

---

## 👥 Application Management

### API Endpoints
```javascript
// Get all applications
GET /api/admin/applications
Authorization: Bearer {token}

// Update application status
PUT /api/admin/application/:id
{ "status": "shortlisted" }

// Get application statistics
GET /api/admin/stats

// Update status with real-time socket
PATCH /api/admin/applicants/status
{ "id": "applicationId", "status": "interview" }

// Get application details
GET /api/admin/applications/:id

// Add note to application
POST /api/admin/applications/:id/notes
{ "note": "Good fit for the role" }
```

### Frontend Implementation - Application List
```jsx
import React, { useState, useEffect } from 'react';
import { useSocket } from './useSocket';

export const ApplicationsList = () => {
  const [applications, setApplications] = useState([]);
  const [filters, setFilters] = useState({ status: 'all' });
  const socket = useSocket();

  useEffect(() => {
    fetchApplications();
  }, []);

  useEffect(() => {
    // Listen for real-time updates
    socket?.on('applicationUpdate', (data) => {
      setApplications(prev => 
        prev.map(app => 
          app.id === data.applicationId 
            ? { ...app, status: data.status }
            : app
        )
      );
    });

    return () => socket?.off('applicationUpdate');
  }, [socket]);

  const fetchApplications = async () => {
    try {
      const response = await api.get('/admin/applications');
      setApplications(response.data);
    } catch (error) {
      console.error('Error fetching applications:', error);
    }
  };

  const updateStatus = async (applicationId, newStatus) => {
    try {
      await api.patch('/admin/applicants/status', {
        id: applicationId,
        status: newStatus
      });
      // UI updates via socket event
    } catch (error) {
      console.error('Error updating status:', error);
    }
  };

  const filtered = applications.filter(app => 
    filters.status === 'all' || app.status.toLowerCase() === filters.status
  );

  return (
    <div className="applications-container">
      <div className="filters">
        <select 
          value={filters.status} 
          onChange={(e) => setFilters({ ...filters, status: e.target.value })}
        >
          <option value="all">All</option>
          <option value="pending">Pending</option>
          <option value="shortlisted">Shortlisted</option>
          <option value="interview">Interview</option>
          <option value="hired">Hired</option>
          <option value="rejected">Rejected</option>
        </select>
      </div>

      <div className="applications-table">
        <table>
          <thead>
            <tr>
              <th>Applicant</th>
              <th>Job</th>
              <th>Score</th>
              <th>Status</th>
              <th>Date</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            {filtered.map(app => (
              <tr key={app.id}>
                <td>{app.applicant?.name || 'Unknown'}</td>
                <td>{app.Job?.title}</td>
                <td>{app.score?.toFixed(2) || '-'}</td>
                <td>
                  <select 
                    value={app.status}
                    onChange={(e) => updateStatus(app.id, e.target.value)}
                  >
                    <option value="pending">Pending</option>
                    <option value="shortlisted">Shortlisted</option>
                    <option value="interview">Interview</option>
                    <option value="hired">Hired</option>
                    <option value="rejected">Rejected</option>
                  </select>
                </td>
                <td>{new Date(app.created_at).toLocaleDateString()}</td>
                <td>
                  <button onClick={() => viewDetails(app.id)}>View</button>
                  <button onClick={() => viewDocuments(app.id)}>Documents</button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
};
```

### Application Pipeline - Kanban View
```jsx
export const ApplicationPipeline = () => {
  const [pipeline, setPipeline] = useState({
    pending: [],
    shortlisted: [],
    interview: [],
    hired: [],
    rejected: []
  });
  const socket = useSocket();

  useEffect(() => {
    loadPipeline();
  }, []);

  useEffect(() => {
    socket?.on('pipelineUpdate', (data) => {
      setPipeline(prev => {
        const updated = { ...prev };
        updated[data.oldStatus] = updated[data.oldStatus].filter(
          app => app.id !== data.applicationId
        );
        updated[data.newStatus].push({
          id: data.applicationId,
          name: data.applicantName
        });
        return updated;
      });
    });
  }, [socket]);

  return (
    <div className="pipeline">
      {Object.entries(pipeline).map(([status, applications]) => (
        <PipelineColumn 
          key={status}
          title={status}
          applications={applications}
        />
      ))}
    </div>
  );
};
```

---

## 💼 Job Management

### API Endpoints
```javascript
// Get all jobs
GET /api/admin/jobs

// Create job
POST /api/admin/jobs
{
  "title": "Software Engineer",
  "description": "Senior backend developer",
  "location": "Toronto, Canada",
  "salary_min": 80000,
  "salary_max": 120000,
  "requirements": "5+ years experience",
  "category_id": 1
}

// Update job
PUT /api/admin/jobs/:id

// Delete job
DELETE /api/admin/jobs/:id

// Get applications for job
GET /api/admin/jobs/:id/applications
```

### Frontend Implementation
```jsx
import React, { useState, useEffect } from 'react';
import { useForm } from 'react-hook-form';

export const JobManagement = () => {
  const [jobs, setJobs] = useState([]);
  const [editingId, setEditingId] = useState(null);
  const { register, handleSubmit, reset, setValue } = useForm();

  useEffect(() => {
    loadJobs();
  }, []);

  const loadJobs = async () => {
    try {
      const response = await api.get('/admin/jobs');
      setJobs(response.data);
    } catch (error) {
      console.error('Error loading jobs:', error);
    }
  };

  const onSubmit = async (data) => {
    try {
      if (editingId) {
        await api.put(`/admin/jobs/${editingId}`, data);
        setEditingId(null);
      } else {
        await api.post('/admin/jobs', data);
      }
      reset();
      loadJobs();
    } catch (error) {
      console.error('Error saving job:', error);
    }
  };

  const handleEdit = (job) => {
    setEditingId(job.id);
    setValue('title', job.title);
    setValue('description', job.description);
    setValue('location', job.location);
    setValue('salary_min', job.salary_min);
    setValue('salary_max', job.salary_max);
  };

  const handleDelete = async (jobId) => {
    if (window.confirm('Delete this job?')) {
      try {
        await api.delete(`/admin/jobs/${jobId}`);
        loadJobs();
      } catch (error) {
        console.error('Error deleting job:', error);
      }
    }
  };

  return (
    <div className="job-management">
      <form onSubmit={handleSubmit(onSubmit)} className="job-form">
        <h2>{editingId ? 'Edit Job' : 'Create New Job'}</h2>
        
        <input {...register('title')} placeholder="Job Title" required />
        <textarea {...register('description')} placeholder="Description" required />
        <input {...register('location')} placeholder="Location" required />
        <input {...register('salary_min')} type="number" placeholder="Min Salary" />
        <input {...register('salary_max')} type="number" placeholder="Max Salary" />
        <textarea {...register('requirements')} placeholder="Requirements" />
        
        <button type="submit">
          {editingId ? 'Update Job' : 'Create Job'}
        </button>
        {editingId && (
          <button type="button" onClick={() => { setEditingId(null); reset(); }}>
            Cancel
          </button>
        )}
      </form>

      <div className="jobs-list">
        <h2>Active Jobs</h2>
        <table>
          <thead>
            <tr>
              <th>Title</th>
              <th>Location</th>
              <th>Salary</th>
              <th>Applications</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            {jobs.map(job => (
              <tr key={job.id}>
                <td>{job.title}</td>
                <td>{job.location}</td>
                <td>${job.salary_min} - ${job.salary_max}</td>
                <td>
                  <a href={`/admin/applications?job=${job.id}`}>
                    View Applications
                  </a>
                </td>
                <td>
                  <button onClick={() => handleEdit(job)}>Edit</button>
                  <button onClick={() => handleDelete(job.id)}>Delete</button>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  );
};
```

---

## 🤖 AI-Powered CV Analysis

### API Endpoints
```javascript
// Analyze single CV
POST /api/admin/cv-scoring/analyze
{ "applicationId": "123", "jobDescription": "Looking for experienced developer" }

// Bulk analyze CVs
POST /api/admin/cv-scoring/bulk-analyze
{ "applicationIds": ["123", "124", "125"], "jobDescription": "Job requirements" }

// Get CV analysis results
GET /api/admin/cv-scoring/results/:applicationId
```

### Frontend Implementation - Single CV Analysis
```jsx
import React, { useState } from 'react';

export const CVAnalyzer = ({ applicationId }) => {
  const [analysis, setAnalysis] = useState(null);
  const [loading, setLoading] = useState(false);

  const analyzeSingleCV = async (appId, jobDesc) => {
    setLoading(true);
    try {
      const response = await api.post('/admin/cv-scoring/analyze', {
        applicationId: appId,
        jobDescription: jobDesc
      });
      setAnalysis(response.data.analysis);
    } catch (error) {
      console.error('Error analyzing CV:', error);
    } finally {
      setLoading(false);
    }
  };

  return (
    <div className="cv-analyzer">
      {loading && <div className="spinner">Analyzing CV...</div>}
      
      {analysis && (
        <div className="analysis-results">
          <div className="score-display">
            <h3>Match Score: {analysis.score.toFixed(2)}%</h3>
            <div className="score-bar">
              <div 
                className="score-fill"
                style={{ width: `${analysis.score}%` }}
              />
            </div>
          </div>

          <div className="skills">
            <h4>Identified Skills</h4>
            <div className="skill-tags">
              {analysis.skills.map(skill => (
                <span key={skill} className="skill-tag">{skill}</span>
              ))}
            </div>
          </div>
        </div>
      )}

      <button onClick={() => analyzeSingleCV(applicationId, 'job description')}>
        Analyze CV
      </button>
    </div>
  );
};
```

---

## 📊 Analytics & Reporting Dashboard

### API Endpoints
```javascript
// Dashboard summary
GET /api/dashboard/summary

// Application statistics
GET /api/dashboard/applications/stats

// Applications over time
GET /api/dashboard/applications/over-time?days=30

// CV score distribution
GET /api/dashboard/cv/score-distribution

// Job application distribution
GET /api/dashboard/applications/by-job

// Interview statistics
GET /api/dashboard/interviews/stats

// Hiring funnel
GET /api/dashboard/funnel

// Payment statistics
GET /api/dashboard/payments/stats

// Top skills
GET /api/dashboard/cv/top-skills

// Time to hire
GET /api/dashboard/time-to-hire
```

### Frontend Implementation - Dashboard Summary
```jsx
import React, { useState, useEffect } from 'react';
import { useSocket } from './useSocket';

export const AdminDashboard = () => {
  const [summary, setSummary] = useState(null);
  const [loading, setLoading] = useState(true);
  const socket = useSocket();

  useEffect(() => {
    loadDashboard();
  }, []);

  useEffect(() => {
    socket?.on('dashboardUpdate', (data) => {
      setSummary(prev => ({ ...prev, ...data.stats }));
    });

    return () => socket?.off('dashboardUpdate');
  }, [socket]);

  const loadDashboard = async () => {
    try {
      const response = await api.get('/dashboard/summary');
      setSummary(response.data);
    } finally {
      setLoading(false);
    }
  };

  if (loading) return <div>Loading...</div>;

  return (
    <div className="admin-dashboard">
      <div className="metrics-grid">
        <MetricCard title="Total Applications" value={summary.totalApplications} icon="📋" />
        <MetricCard title="Active Jobs" value={summary.totalJobs} icon="💼" />
        <MetricCard title="Scheduled Interviews" value={summary.totalInterviews} icon="📹" />
        <MetricCard title="Hired" value={summary.totalHired} icon="✅" />
        <MetricCard title="Average Score" value={summary.averageScore.toFixed(2)} icon="⭐" />
      </div>
    </div>
  );
};

const MetricCard = ({ title, value, icon }) => (
  <div className="metric-card">
    <div className="metric-icon">{icon}</div>
    <div className="metric-content">
      <p className="metric-title">{title}</p>
      <h3 className="metric-value">{value}</h3>
    </div>
  </div>
);
```

---

## 📧 Communication & Email Management

### Email API Endpoints
```javascript
// Send email to individual applicant
POST /api/admin/send-email
{
  "recipientId": "applicantId",
  "subject": "Interview Invitation",
  "template": "interview_invite",
  "variables": { "interviewTime": "2024-04-15 10:00 AM" }
}

// Send bulk email
POST /api/admin/send-bulk-email
{
  "recipientIds": ["id1", "id2", "id3"],
  "subject": "Application Update",
  "template": "status_update",
  "variables": {}
}

// Get email templates
GET /api/admin/email-templates

// Create custom email template
POST /api/admin/email-templates
{
  "name": "custom_template",
  "subject": "Subject line",
  "body": "Email content {{variable}}"
}
```

### Features
- **Individual Email Communication**: Send personalized emails
- **Template Selection**: Choose from predefined templates
- **Rich Text Formatting**: HTML email composition
- **Delivery Tracking**: Monitor email delivery and open rates
- **Bulk Campaigns**: Send emails to multiple applicants
- **Personalization**: Dynamic content insertion

---

## 📋 Interview Management

### Interview API Endpoints
```javascript
// Create interview
POST /api/admin/interviews
{
  "applicationId": "123",
  "interviewType": "video",
  "scheduledTime": "2024-04-15T10:00:00Z"
}

// Update interview status
PUT /api/admin/interviews/:id
{ "status": "completed", "score": 8.5 }

// Add interview feedback
POST /api/admin/interviews/:id/feedback
{ "feedback": "Good communication skills", "score": 8.5 }

// Get interview details
GET /api/admin/interviews/:id
```

### Features
- **Interview Creation**: Schedule interviews
- **Date/Time Management**: Set interview slots
- **Interview Types**: Video, voice AI, in-person
- **Status Monitoring**: Track progress
- **Feedback Collection**: Record evaluations
- **Score Recording**: Save assessments

---

## 🔐 Security & Audit

### Audit Logging
- Activity tracking for all administrative actions
- User attribution for every action
- Timestamp recording
- Compliance reports generation

### Data Protection
- File encryption with AES-256
- Role-based access controls
- Secure storage
- GDPR compliance

---

## 📡 Real-Time Features

### Live Dashboard Updates
- Real-time metrics
- Application tracking
- Interview updates
- WebSocket integration

### Event Broadcasting
- Admin notifications
- Status changes
- New applications
- System alerts

---

# COMPLETE FEATURE LIST

## 🎯 **Authentication & User Management**
- User Registration (Email/password with OTP verification)
- Multiple Login Options (Traditional + OTP)
- Password Management (Forgot, Reset, Change)
- Profile Management (View/edit profile, upload CV, update information)
- Session Management (JWT token handling, auto-refresh, logout)
- Role-Based UI (Different interfaces for users vs admins)
- Two-Factor Authentication (Optional enhanced security)

## 💼 **Job Portal Features**
- Job Browsing (Public listings with pagination)
- Job Details (Requirements, salary, location)
- Job Categories (Browse by category)
- Advanced Search (Multiple filters)
- Saved Jobs / Bookmarking
- Job Alerts (Email notifications)
- Job Recommendations (AI-powered matching)

## 📝 **Application Management**
- Job Application (Upload CV, cover letter, documents)
- Application Tracking (View status, updates)
- Document Management (Upload passport, ID, certificates)
- Application History (Timeline, communications)
- Application Withdrawal (Before offer stage)
- Bulk Application Management (Admin bulk updates)

## 🤖 **AI-Powered Features**
- CV Analysis (Skill extraction, scoring)
- AI Interview Bot (Interactive interviews)
- Job Matching (CV-based recommendations)
- Skill Assessment (Automated evaluation)
- Resume Optimization (Improvement suggestions)
- CV Scoring Bulk Analysis

## 📹 **Interview Management**
- Interview Scheduling (View schedules)
- Video Interviews (WebRTC-based)
- Voice Interviews (AI-powered)
- Interview Feedback (Scores, comments)
- Interview History (Recordings, transcripts)
- Reschedule Requests
- Interview Analytics

## 👨‍💼 **Admin Dashboard**
- Application Pipeline (Kanban view)
- Bulk Actions (Bulk status updates)
- CV Scoring Interface
- Interview Management
- Email Campaigns (Bulk messaging)
- Offer Generation (With salary details)
- User Management (Create/modify admins)

## 📊 **Analytics & Reporting**
- Dashboard Overview (Key metrics)
- Application Analytics (Trends by job/status)
- CV Analytics (Score distribution, skills)
- Interview Analytics (Completion rates)
- Hiring Funnel (Conversion rates)
- Time-to-Hire Metrics
- Financial Reports (Revenue by service)
- Custom Reports

## 💳 **Payment Integration**
- Application Fees
- Interview Fees
- Visa Processing Payments
- Payment History
- Refund Management
- Receipt Generation

## 📧 **Communication & Notifications**
- Email Notifications (Status, invites, offers)
- In-App Notifications (Real-time)
- Bulk Messaging
- Interview Reminders
- Feedback Collection

## 🔴 **Real-Time Features**
- Live Updates (Socket.io WebSockets)
- Live Application Status
- Live Dashboard Metrics
- Live Interview Status
- Live Notifications
- Live Applicant Tracking

## 📱 **Responsive Design**
- Mobile-First Optimization
- Tablet Support
- Desktop Experience
- Progressive Web App
- Cross-Browser Compatibility

## 🔒 **Security & Compliance**
- Data Encryption (AES-256)
- GDPR Compliance
- Audit Logging
- Secure File Upload
- Access Controls
- Privacy Settings

---

# SECURITY & BEST PRACTICES

## API Security

1. **Always use HTTPS** in production
2. **Validate all inputs** on both frontend and backend
3. **Implement CORS** properly for allowed origins
4. **Use JWT tokens** with expiration
5. **Refr tokens separately** from access tokens
6. **Don't expose sensitive data** in frontend code
7. **Sanitize user inputs** to prevent XSS
8. **Use rate limiting** to prevent abuse
9. **Implement request signing** for sensitive operations
10. **Log all security events** for audit trails

## Frontend Security

1. **Store tokens securely** (httpOnly cookies preferred)
2. **Clear sensitive data** after logout
3. **Use Content Security Policy** headers
4. **Implement CSRF protection**
5. **Validate file uploads** (type, size)
6. **Encrypt sensitive documents**
7. **Use secure password reset** flow
8. **Implement timeout for inactive sessions**
9. **Show security warnings** appropriately
10. **Keep dependencies updated**

## Data Protection

1. **Encrypt sensitive files** before upload
2. **Use secure protocols** for file transfer
3. **Implement access controls** for documents
4. **Maintain audit logs** for all access
5. **Purge old data** according to retention policy
6. **Backup data regularly** with encryption
7. **Restrict admin access** appropriately
8. **Monitor for suspicious activity**
9. **Implement data classification**
10. **Follow GDPR requirements**

---

## 📞 Support

For questions or issues:
1. Check the relevant guide section above
2. Review error messages carefully
3. Check browser console for errors
4. Test endpoints using provided test files
5. Ensure proper authentication headers
6. Verify API URL configuration
7. Check network connectivity

---

**Last Updated**: April 6, 2026
**Version**: 2.0 (Combined Guides + User Section)
