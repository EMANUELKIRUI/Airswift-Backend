import React from 'react';
import RegisterPage from '../components/RegisterPage_GMAIL_VALIDATED';

/**
 * ✅ REGISTER PAGE
 * Registration page route that renders the RegisterPage component
 * Allows new users to create accounts with Gmail validation
 */
export default function RegisterPageRoute() {
  return <RegisterPage />;
}
